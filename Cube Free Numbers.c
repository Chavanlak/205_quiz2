#include<stdio.h>
    int i,j,n,x,k;
    for(i=n;i<=n;i++)
    {
        if(k[i]>0)
        {
           x=i*i*i;
           for(j=x;j<=1000000;j+=x)
           k[j]=-1;
        }

            for(i=1;i<=1000000;i++)
                if(k[i]=x++)
    }
    int main()
    {
        cube_freenumber();
        int t,y;
        scanf("%d",&t);
        for(i=1;i<=t;i++)
        {
            scanf("%d",&y);
            if()
            {

            }
            else
                printf("");
        }

    }














}
